spring:
  mvc:
    servlet:
      load-on-startup: 1
      path: /api
webui:
  servlet:
    load-on-startup: 1
    path: /*
  configuration-type: DEVELOPMENT
  csrf: false
  wicket-factory: # Wicket Factory Class
  pkg: # Pages packages
  admin-lte: # Admin LTE
